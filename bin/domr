#!/usr/bin/env ruby
require 'domr'

# Check for empty query
if !ARGV[0]
	puts "Please enter a name".color(:red).bright
	
	# Exit the program
	exit 1
end

# Perform the search
if Domr.search(ARGV[0])
	exit 1
end